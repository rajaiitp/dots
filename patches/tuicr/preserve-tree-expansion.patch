diff --git a/src/app/navigation.rs b/src/app/navigation.rs
index 2fec63e..23557c9 100644
--- a/src/app/navigation.rs
+++ b/src/app/navigation.rs
@@ -579,8 +579,6 @@ impl App {
     }
 
     pub fn jump_to_file(&mut self, idx: usize) {
-        use std::path::Path;
-
         if idx < self.diff_files.len() {
             // Deliberate jump cancels any in-flight two-press walk arming.
             self.primed_walk_next = false;
@@ -597,16 +595,9 @@ impl App {
             let max_scroll = self.max_scroll_offset();
             self.diff_state.scroll_offset = self.diff_state.cursor_line.min(max_scroll);
 
-            let file_path = self.diff_files[idx].display_path().clone();
-            let mut current = file_path.parent();
-            while let Some(parent) = current {
-                if parent != Path::new("") {
-                    self.expanded_dirs
-                        .insert(parent.to_string_lossy().to_string());
-                }
-                current = parent.parent();
-            }
-
+            // Directory expansion is controlled only by explicit tree actions.
+            // If this file is hidden by a collapsed parent, leave the tree
+            // selection on that parent instead of reopening it automatically.
             if let Some(tree_idx) = self.file_idx_to_tree_idx(idx) {
                 self.file_list_state.select(tree_idx);
             }
