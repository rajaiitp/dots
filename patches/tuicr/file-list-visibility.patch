--- a/src/handler.rs	2026-07-24 17:29:50
+++ b/src/handler.rs	2026-07-24 17:29:50
@@ -1413,7 +1413,7 @@
             }
         }
         Action::ExitMode => {
-            app.show_file_list = false;
+            // Escape returns focus to the diff without hiding the file list.
             app.focused_panel = FocusedPanel::Diff;
         }
         Action::HalfPageDown => app.scroll_down(app.diff_state.viewport_height / 2),
--- a/src/main.rs	2026-07-24 17:29:50
+++ b/src/main.rs	2026-07-24 17:29:50
@@ -284,8 +284,14 @@
         }
     }
 
-    // On narrow terminals, start with only the diff panel visible.
-    if let Ok((width, _)) = crossterm::terminal::size()
+    // Keep the file list visible when explicitly enabled in config, even on
+    // narrow terminals. Otherwise, retain the narrow-layout default.
+    if config_outcome
+        .config
+        .as_ref()
+        .and_then(|cfg| cfg.show_file_list)
+        != Some(true)
+        && let Ok((width, _)) = crossterm::terminal::size()
         && width < MIN_WIDTH_FOR_FILE_LIST
     {
         app.show_file_list = false;
